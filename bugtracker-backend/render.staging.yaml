version: "1"
projects:
- name: Bugtracker-Kamui
  environments:
  - name: Staging
    services:
    - type: web
      name: bugtracker-backend-staging-kamui
      runtime: go
      repo: https://github.com/kamui-git/bug-tracker
      plan: free
      envVars:
      - fromGroup: bugtracker-staging-env
      region: singapore
      buildCommand: go build -tags netgo -ldflags '-s -w' -o app ./cmd/server
      startCommand: ./app
      healthCheckPath: /api/health
      autoDeployTrigger: checksPass
      rootDir: ./bugtracker-backend
